---
"cortex-forge": minor
---

`cortex-recall` offers to persist genuinely new synthesis (rarely, never on single-page lookups) and logs recall misses to `wiki/meta/log.md`. `cortex-prune`'s L2a now also flags factual contradictions between related pages in the same pass it checks for missing wikilinks, and a new L2e surfaces recurring recall misses as `/cortex-assimilate` candidates.
