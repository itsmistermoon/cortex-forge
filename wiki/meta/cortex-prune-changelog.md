# cortex-prune — skill changelog

Change history for `skills/cortex-prune/SKILL.md`, moved out of the skill file to keep it lean. Newest first.

---

- 2026-07-05 [Claude Code]: Fixed a real, silent bug in scripts/cortex-validate-schema.sh — `walk "pages" "$fields_project"` checked a directory (`wiki/pages/`) that hasn't existed since the 2026-07-04 rename to `wiki/projects/`; that rename updated the SKILL.md files but missed this companion script, so `wiki/projects/*.md` pages were never schema-validated (the `walk` function returns silently when the directory is missing — no error, no warning). Fixed to `walk "projects"`. Verified against the live vault: now correctly walks `wiki/projects/cortex-forge.md` and finds no drift (all 11 required fields present). Also removed the `walk "reference" ...` call — the script's own comment two lines above already says "reference and series are retired"; the directory was deleted 2026-07-04 when `wiki/reference/` merged into `wiki/concepts/`, so this walk was dead code contradicting the script's own documentation
- 2026-07-05 [Claude Code]: Closed a real remediation gap — 3 of 7 Detection criteria checks (dead links, `.raw/` without source page, missing frontmatter) plus concepts/entities missing `sources:` had no policy in either Auto-correctable or Requires confirmation; they were detected and reported but the skill never said what to do about them. Generalized the `confidence:` auto-correction to any page type (was source-pages-only) and added 4 new Requires-confirmation bullets, each with a concrete proposed action rather than a bare "report only" note — dead link → propose retargeting or removal; `.raw/` without source → propose invoking `/cortex-assimilate` inline within the same prune session; missing frontmatter → propose a best-effort draft; missing `sources:` on concepts/entities → propose candidates from existing wikilinks/mentions. Every finding in Detection criteria now resolves to either Auto-correctable or Requires confirmation
- 2026-07-05 [Claude Code]: Removed Rules' "`index.md` and `log.md` without frontmatter is expected" — duplicated the Detection criteria table's own "(excl. `index.md`, `log.md`)" exclusion; kept only in the table
- 2026-07-05 [Claude Code]: Fixed a stale comment in scripts/cortex-prune.sh — "Canonical schema defined in ... SKILL.md step 4a" pointed at a step that no longer exists after the renumbering; now points to references/VAULT-REPORT-SCHEMA.md
- 2026-07-05 [Claude Code]: Renamed Layer 2 check labels 2a/2b/2c/2d → L2a/L2b/L2c/L2d across all 12 mentions (headings, Hard cap, Rules, Auto-correctable, Requires confirmation) — disambiguates them from step numbers now that steps also reach into the 2-4 range
- 2026-07-05 [Claude Code]: Fixed a real correctness bug: step 3 and ## Rules both said "For Layer 2, spawn subagents" — directly contradicting all four Layer 2 sub-checks (2a-2d), which explicitly say "evaluate inline, do not spawn subagents for this step," and the Hard cap section, which already says inline is preferred for small vaults with an optional 3-subagent budget for large ones. Corrected both to "read the actual pages, never reason from memory alone" — no longer contradicts the sub-checks, and doesn't duplicate the Hard cap section's subagent policy. Evaluated whether Layer 2 should fold into step 3 as inline sub-steps or extract to references/ — kept it where it already was (adjacent to ## Steps, not extracted): it runs on every invocation (no branching skip), so extracting would relocate weight without reducing it, and no internal redundancy was found to compress
- 2026-07-05 [Claude Code]: Promoted "3a. Layer 3 — Drift detection" to its own top-level step (4) — it's a sibling of Layer 1/2, not a sub-step of Layer 2 (step 3); folded the old "4a. Verify vault-report.json" into the new step 5 (Report) as a bullet, extracting its JSON schema + field definitions to new references/VAULT-REPORT-SCHEMA.md (that content was static reference documentation, not a per-run decision); renumbered Consolidate (5→6) and Ask (6→7) accordingly
- 2026-07-05 [Claude Code]: Added "Paths are relative to this skill's directory." to ## Available scripts (present in other suite skills, missing here) and trimmed the resulting 3x-repeated "cortex-prune.sh is co-located with this skill" detail from step 2, the Detection criteria heading, and Auto-correctable's vault-report.json line
- 2026-07-05 [Claude Code]: Removed Rules' "Never delete or merge pages without explicit user confirmation" — fully covered by ## Requires confirmation, which lists the exact same cases (merge, delete orphan pages) with more precision
- 2026-07-05 [Claude Code]: Reworded step 1 to match the suite-wide "Resolve vault" phrasing, dropping the no-op "(argument → CWD → default)" parenthetical already covered by VAULT-RESOLUTION.md
- 2026-07-05 [Claude Code]: Removed the AGENTS.md ## Vault identity dependency (section eliminated from the suite): dropped the Domains/Out of scope check from step 1
- 2026-07-05 [Claude Code]: Compressed the flavor-line instruction to a single sentence — removed the duplicated position instruction and the "literally" vs "translated" contradiction
- 2026-07-04 [Claude Code]: Centralized vault structure validation (`wiki/`+`AGENTS.md`) in `references/VAULT-RESOLUTION.md`; removed the now-redundant explicit "Confirm vault is a Cortex Forge vault" line from step 1 (confirmation gate left untouched)
- 2026-07-04 [Claude Code]: Reworded "Resolve vault" step intro to point directly at VAULT-RESOLUTION.md's decision flow, removing the vague closing phrase
- 2026-07-04 [Claude Code]: Extracted "Resolve vault" logic to shared `references/VAULT-RESOLUTION.md`, co-located across 5 skills (was duplicated inline with real drift between copies)
- 2026-07-01 [Claude Code]: Added Layer 3 — drift detection: mtime of `.raw/` vs `updated:` in `wiki/sources/`; intro updated from two to three layers
