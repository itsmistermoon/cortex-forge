# Changelog

Protocol-significant changes to cortex-forge are documented here.

**What counts as protocol-significant:**

- Changes to `AGENTS.md` compliance criteria or session startup sequence
- Changes to skill contracts (input, output, steps, compliance criteria)
- Changes to template frontmatter schema
- Changes to `vault-report.json` schema
- New files added to `bin/` or `docs/` that alter vault operation
- Changes to `~/.cortex-forge/config.yml` structure

**What does not count:** rewording, typos, README prose, cosmetic changes.

Format: `[semver] — title — YYYY-MM-DD`

**If an `[Unreleased]` section exists, it must always be the first section in this file, above every `[semver]` header** — Changesets prepends each new version block at the very top of the file (right after the `# Changelog` title) with no awareness of anything hand-written below it. An `[Unreleased]` section left under the latest version gets buried deeper on every subsequent release instead of being picked up, exactly what happened to the `AGENTS.md` protocol-trim entry that sat orphaned until it was folded into 0.8.0. When cutting a release, either move `[Unreleased]` back to the top first, or fold its content into the new version entry if it already shipped.

---

## [0.8.0] — Tag Governance, Vault Scaffolding & Plugin Distribution — 2026-07-12

Wires the tag-governance infrastructure that shipped inert in prior releases into the 3 skills that actually use it, lets `cortex-forge-setup` scaffold a brand-new vault instead of requiring one to already exist, closes a `cortex-prune` performance cliff that made it unusable on vaults of a few hundred pages, and adds a plugin-marketplace distribution channel alongside the existing `npx skills add`.

- `protocol:` Tag governance wired into 3 skills: `cortex-assimilate` validates proposed tags against `wiki/meta/tags.md` before writing a page; `cortex-prune` Layer 1 flags tags used exactly once with no backing page; `cortex-forge-setup`'s maintenance menu gains an option to run `bin/tags-audit.py`. None auto-mutate — all surface findings for human decision.
- `feat:` `cortex-forge-setup` can scaffold a brand-new vault's `wiki/` structure and a starter `AGENTS.md` instead of requiring them to pre-exist. `templates/tags.md` added as a 5th template.
- `protocol:` `cortex-recall` offers to persist genuinely new synthesis and logs recall misses to `wiki/meta/log.md`; `cortex-prune` L2a flags factual contradictions between related pages, and a new L2e surfaces recurring recall misses as `/cortex-assimilate` candidates.
- `feat:` `cortex-assimilate` accepts pasted text as a source (no fetchable URL) and falls back to it automatically when a fetch fails, for sites that block scraping or sit behind a paywall.
- `fix:` `cortex-prune`'s orphan-page and unprocessed-`.raw/`-file checks were O(n × m) and never finished on a 353-page vault (10+ min) — rewritten to a single-pass index (~17s). Also fixed a pre-existing parser bug that misattributed wikilinks across an unrecognized `index.md` heading.
- `fix:` Fixed a latent `embeddings.py` bug: the `mlx-embeddings` backend pointed at a non-existent model, silently masked as "MLX unavailable."
- `feat:` Packaged the skill suite as a self-hosted Claude Code plugin marketplace (`/plugin marketplace add itsmistermoon/cortex-forge`), alongside the existing `npx skills add` distribution.
- `protocol:` `wiki/meta/log.md` entries switched from `## [YYYY-MM-DD]` (H2 heading) to `**[YYYY-MM-DD]**` (bold) — the heading cluttered document outlines in Obsidian.
- `protocol:` `AGENTS.md`'s three "{Crystallize,Assimilate,Recall} protocol — MANDATORY" sections (123 lines) collapsed into one 3-line "Session start" section — each skill already states when to trigger via its own `description:`, so `AGENTS.md` no longer duplicates that.

---

## [0.7.0] — npx-only Distribution, Security Hardening & Suite Compaction — 2026-07-06

Eliminates all local vault code execution (scripts skills invoke now live co-located inside each `skills/<name>/`, never copied to `{vault}/.cortex/db/`), drops the tarball/curl-installer in favor of `npx skills add` (skills.sh) as the sole distribution channel, and closes out the session with a full compaction pass across all 6 skills — less text, zero loss of decision logic, and several real bugs fixed along the way (contradictory instructions, broken references, a script that silently stopped validating `wiki/projects/`).

- `fix:` — 2026-07-06 — CodeRabbit review (`coderabbit review --agent --base v0.6.0`) against the full diff accumulated since v0.6.0, each finding verified against the real code before applying. Confirmed and fixed: `cortex-prune.sh` counted a wikilink as a hit on prefix overlap (`[[wiki/concepts/foo-bar]]` counted as a hit for `foo`) and used `\s` (a GNU extension, not supported by macOS's BSD grep) in the `sources:` fallback — both silenced false negatives in orphan detection. `cortex-crystallize/SKILL.md` pointed PRAXIS pruning at `### Working context` (a heading that doesn't exist; the real structure is `## Working context` with `### YYYY-MM-DD` subsections), leaving the pruning step with no effect. `VAULT-RESOLUTION.md` (co-located across 5 skills) didn't handle the "no default, exactly one vault registered" case and asked unnecessarily. `cortex-forge-setup/SKILL.md`: step 4 said the skills check was "implicit" instead of actually checking each one; steps 3a/3c didn't spell out preserving other top-level keys (`upstream:`, `imprint_triage:`, etc.) when rewriting `config.yml`. `cortex-search.py` (cortex-recall) had a docstring copy-pasted from cortex-forge-setup, didn't handle `sqlite3.OperationalError` when loading the sqlite-vec extension (crashed instead of falling back to `import sqlite_vec`), and with an explicit `--vault` could create an empty `vault.db` instead of failing with "Run cortex-index first" — same extension fix applied to `cortex-index.py` (assimilate and forge-setup). `cortex-recall/SKILL.md` only checked the canonical index path, ignoring the legacy layout `cortex-search.py` still supports. `bin/check-skill-sync.sh` hardcoded the 4 `vault-report.json` field names instead of discovering them structurally from `VAULT-REPORT-SCHEMA.md` — a newly added field would have been silently skipped, the exact class of bug this check exists to prevent. Plus 3 minor content fixes in `README.md` and `wiki/`.
- `refactor:` — 2026-07-06 — Full compaction of `cortex-forge-setup/SKILL.md` (156 → ~118 lines): standardizes `## Available scripts` and step 1 ("Resolve context" → "Validate vault candidate") to the pattern already used in the other 5 skills; removes a quadruplication of "npx skills add is the sole installer"; removes `## Memory model` entirely (a development-decision rationale — why not use lifecycle hooks — irrelevant to the install process, not just de-duplicated but removed outright per explicit request); merges steps 5b+5c (post-commit hooks) into one; extracts step 7 ("Confirm result") into its own `## Output format`; extracts the entire maintenance menu (formerly step 2b, genuinely conditional content never read on a new-vault run) to `references/MAINTENANCE-MODE.md`, with its 9 options reordered to ascending step order. Fixes a real contradiction in `## Rules` ("read only" when 3 steps do write) by scoping it to "never writes to `wiki/`, `.raw/`, or `AGENTS.md`". Fixes three real bugs found in cross-review with Fable and Gemini (independent, no conversation context, findings verified one by one before applying): `EMBEDDING-SETUP.md` imported `mlx_lm` instead of `mlx_embeddings` with a broken `&&`/`||` chain that printed "mlx" and "st" simultaneously; Ollama detection didn't distinguish "never installed" from "installed but not running"; `UPSTREAM-SYNC.md` never wired `upstream_ref:` (documented as configurable) into the one API call that needed it (URL hardcoded `main`). Removes `## AGENTS.md structural check` entirely — assuming a vault's `AGENTS.md` resembles the upstream template closely enough for a rigid structural diff contradicts skills needing to stand on their own. Wires "Uninstall (deregister path)" (documented in `POST-COMMIT-HOOKS.md` but with no trigger anywhere in the project) to maintenance menu option 9.
- `feat:` — 2026-07-06 — The translated flavor-line rule (see the 2026-07-04 entry below) is generalized across all 6 skills: previously it only covered that first line; now it covers every prompt, question, menu, and confirmation a skill produces, with the explicit exception that content persisted to the vault still follows the vault's `locale:`, not the conversation language. `cortex-forge-setup` gets its flavor line for the first time (it never had one).
- `refactor:` — 2026-07-05 — Removed the `## Vault identity` section from `AGENTS.md` (a redundant stub — locale is already resolved via `config.yml`/`MEMORY.md`); `LOCALE-RESOLUTION.md` no longer references it in the 3 co-located skills (`cortex-assimilate`, `cortex-crystallize`, `cortex-imprint`); `cortex-forge-setup` no longer offers the step to write/rewrite the stub. `cortex-assimilate`'s `--research` mode now saves each source to `.raw/` with the same SPA/sanitization checks as the normal flow and runs the full steps 3-7 (previously 3-5, without backward enrichment or project linking); type/path table and disambiguation merged into `## Page types`. Moved the `## Changelog` history from the 5 skills that had one (`cortex-assimilate`, `cortex-crystallize`, `cortex-imprint`, `cortex-prune`, `cortex-recall`) to `wiki/meta/{skill}-changelog.md` — reviewed each skill's git history to confirm no historical entry was lost.
- `refactor:` — 2026-07-05 — Compaction of `cortex-crystallize` (SKILL.md + its own references: 242→164 lines, -32%; 2705→1581 words, -42%). `SKILL.md`: removes `## Modes` and `## Zone 1/2` entirely (duplicated by `MEMORY-FORMAT.md`, `PRAXIS-FORMAT.md`, and `AGENTS.md`); collapses the detection/preparation steps into two continuous-prose steps; merges "Update current state" and "Append snapshot to history" into a single "Write the snapshot"; redesigns the two Cross-vault update questions so the agent proposes (scanning the session for pages actually consulted) instead of asking blind; removes `## Rules` entirely (two of its three rules duplicated `LOCALE-RESOLUTION.md`/step 2, the one with real value moves to `MEMORY-FORMAT.md`). `MEMORY-FORMAT.md`: removes `## Trigger types` entirely (the `{Trigger}` placeholder in the History header is also removed), drops `### Suggested skills` as a rigid section, folds `### Discarded` inline into `### What was done`. `PRAXIS-FORMAT.md`: removes `## When to write` entirely (duplicated `SKILL.md` step 3's framing), distributes the classification gate (Confirmed/Provisional/Ambiguous) directly into Zone 1/2. TTL changed from 30 to 15 days across the 3 remaining mentions in the ecosystem. Evaluated and rejected splitting into a separate `cortex-maintenance` skill (proposed by Gemini 3.1 Pro, reviewed adversarially): the PRAXIS classification step concentrated 37% of the text weight and was the only one requiring a full session re-read; splitting the candidate steps (History rotation, PRAXIS pruning, health triage) would have left their trigger orphaned.
- `fix:` — 2026-07-05 — Compaction of `cortex-imprint/SKILL.md` (81 → 50 lines, -38%). Fixes two factually incorrect cross-references (the "circular synthesis" test actually lived in Source hierarchy, not in Rules as Constraints claimed; step 2 pointed to "(see Rules)" instead of Source hierarchy). Removes `## Constraints` entirely (100% duplicated the Source hierarchy test) and 3 duplicated bullets from `## Rules`. Merges 8 steps into 5 without losing any instruction. Fixes a real bug in the type table: "page (ADR)" didn't match the real template (`templates/project.md`) — now says "project". Aligns step 1's wording in `cortex-assimilate` and `cortex-imprint` with `cortex-crystallize`'s for consistency across all 6 skills.
- `fix:` — 2026-07-05 — `cortex-prune`: fixes a real contradiction — step 3 and `## Rules` said "spawn subagents" for Layer 2, contradicting the 4 checks (2a-2d, renamed L2a-L2d) that explicitly evaluate inline. Promotes Layer 3 (formerly sub-step "3a") to its own step; extracts the `vault-report.json` schema to `references/VAULT-REPORT-SCHEMA.md`. Closes a real remediation gap: 3 of 7 Detection criteria checks were detected and reported but the skill never said what to do about them — each now resolves to a concrete proposed action under Requires confirmation. Fixes a real bug in `cortex-validate-schema.sh`: `walk "pages"` pointed at a directory that hasn't existed since the `wiki/pages/`→`wiki/projects/` rename on 2026-07-04 (that rename updated the `SKILL.md` files but not this companion script, silently leaving `wiki/projects/` unvalidated) — fixed to `walk "projects"`, verified against the real vault. Removes the "reference" walk (a type retired since 2026-07-04), dead code.
- `fix:` — 2026-07-05 — `cortex-assimilate`/`embeddings.py`: `DIMENSIONS` is now validated against the real vector each backend returns before saving it (previously declared but never used; `cortex-index.py` hardcoded 768 in two places in parallel). Changes the Ollama timeout from a direct `sys.exit(1)` (killed the entire indexing run on a single transient failure) to a catchable `EmbeddingBackendError` exception — `cortex-index.py` now catches it per-file inside the main loop, reports which ones failed, and continues with the rest; re-running the command only retries the failures. Verified against the real vault: full indexing, semantic query, and a simulated failure confirming correct rollback (zero orphan rows) and continuation of the batch without crashing. `embeddings.py`, `cortex-index.py`, and their intentional duplicate copies in `cortex-recall` and `cortex-forge-setup` (each skill must work standalone if installed individually) synced.
- `refactor:` — 2026-07-05 — `cortex-recall`: fixes a real triplication caught by our own `wiki/concepts/skill-design-principles.md` — "reading wiki/index.md directly is not a bypass" repeated across step 2, `## Constraints`, and `## Rules`; kept only where it operationally applies (step 2). Merges the "Read full pages" + "Synthesize response" + "Suggest ingestion" steps into a single "Answer" step. `## Output format` from bullets to a numbered list, consistent with `cortex-assimilate`/`cortex-crystallize`. SKILL.md: 54 → 48 lines (-11%), 576 → 485 words (-16%).
- `feat:` — 2026-07-04 — The flavor line ("Begin your response by outputting exactly: X...") was hardcoded in English across all 6 skills. It's ephemeral chat output, never persisted to the vault or parsed by any script/hook/CI (verified) — unlike content that does follow `locale:` (wiki pages, `MEMORY.md`, `PRAXIS.md`), it should match the language the user is currently writing in, not the vault's configured locale. Tested first in `cortex-crystallize`, then extended to `cortex-assimilate`, `cortex-imprint`, `cortex-prune`, and `cortex-recall`.
- `feat:` — 2026-07-04 — `cortex-crystallize` gains an explicit "safe to end" verdict and a "genuinely ambiguous" gate before writing to `PRAXIS.md` (ask-once, not an interrogation); a new vault-health triage step propagates `vault-report.json` to `### Pending` non-optionally. `AGENTS.md` excepts vault-health triage from the general "acknowledge or defer" rule for `### Pending`, since it's system health information, not a user task.

- `fix:` — 2026-07-05 — `bin/check-skill-sync.sh` had real drift after the skill compaction session (crystallize, imprint, prune, recall): the `vault-report-schema` check crashed the whole script (`set -e`+`pipefail` on a `grep` that no longer found anything, since the JSON schema moved from `cortex-prune/SKILL.md` to `cortex-prune/references/VAULT-REPORT-SCHEMA.md`), and the `colocated-script-exists` check relied on the literal word "co-located" to know which scripts to verify — a word removed from the 5 reviewed skills in favor of a single convention line ("Paths are relative to this skill's directory"), leaving the check passing vacuously without checking anything for those 5. Fixed: `vault-report-schema` points at the new reference file with defensive guards; `colocated-script-exists` was redesigned as `available-script-exists`, structurally scanning the `## Available scripts` section instead of depending on a keyword. The old `prune-script-colocated` check (which verified the same thing, but only for cortex-prune, with the same now-removed keyword) was removed entirely — `available-script-exists` already covers that case generically for all 6 skills. Verified with a real negative test (temporarily renaming `cortex-validate-schema.sh`) that the check correctly detects the regression before restoring.
- `feat:` — 2026-07-05 — Re-implemented the "stale cache" warning (`hot_cache_stale_days:` in `~/.cortex-forge/config.yml`, global), orphaned since the `SessionStart` hook was retired on 2026-07-02. Now lives as step 2 (new) of `AGENTS.md`'s mandatory read protocol: compares `updated:` in `MEMORY.md`'s frontmatter against the current date and, if it exceeds the threshold (default 15 days), surfaces it in the first message of the session. `skills/cortex-forge-setup/SKILL.md` gains optional step 3c (and maintenance menu option 9) to configure the value — global, not per-vault, with a notice if it was already set instead of asking as if for the first time.
- `fix:` — 2026-07-05 — `ROADMAP.md` described agent lifecycle hook mechanisms already retired on 2026-07-02 (Phase 1: Claude Code/Antigravity/CommandCode/Codex; Phase 2: hook distribution architecture, imprint pipeline with triage/auto-mode) as if still in effect. Fixed all affected lines to reflect the current manual protocol via `AGENTS.md`; the "stale cache detection" line was restored to checked (see previous entry). No other roadmap line was touched.
- `fix:` — 2026-07-05 — Residual mention of "SessionStart" in `ROADMAP.md` (the "Link-count scan — orphan page detection" line), missed in the earlier Phase 1/2 fix. Fixed to the same pattern: `orphan_pages` in `vault-report.json` surfaces via `AGENTS.md`'s mandatory read protocol, not a `SessionStart` hook.

- `fix:` — 2026-07-04 — Removed `wiki/meta/agent-diagnostics.md` — a diagnostics log for an agent lifecycle hook subsystem retired on 2026-07-02. Its only still-current finding (agent detection via process tree) was already rescued into step 1a of `skills/cortex-crystallize/SKILL.md`.

- `fix:` — 2026-07-04 — `README.md` (`## Design rationale`) claimed in future tense two features already implemented: ingestion-time sanitization (`*(sanitization landing in v0.4)*`, already wired via `skills/cortex-assimilate/scripts/cortex-sanitize.sh` in `cortex-assimilate/SKILL.md` step 4a) and the "Attempted and failed" section of the hot cache contract (`*(landing in v0.4)*`, already present in `skills/cortex-crystallize/references/MEMORY-FORMAT.md`). Both corrected to present tense. Deliberately left untouched the claim about "imprint pipeline landing in v0.4" on the same line — pending separate review, since the mechanism it describes was replaced.

- `fix:` — 2026-07-04 — The multi-vault config example in `README.md` (`## Multi-vault`) used the obsolete flat schema (`vaults: {name: path}`). Fixed to the real nested schema already used in `~/.cortex-forge/config.yml` and documented consistently across all 5 skills: `vaults: {name: {path, locale}}`.

- `fix:` — 2026-07-04 — `wiki/pages/` was renamed to `wiki/projects/` across the vault and skills, aligning the code with the taxonomy already documented in `README.md` (`## Wiki Taxonomy`, "Project | `wiki/projects/`" row). The code used `wiki/pages/` while the docs already said `wiki/projects/`; decided to align the code to the documented name. Moved `wiki/pages/cortex-forge.md` to `wiki/projects/cortex-forge.md` with `git mv`, updated all `[[wiki/pages/*]]` wikilinks and operational mentions in `AGENTS.md`, `wiki/index.md`, `wiki/meta/*`, and the `cortex-assimilate`, `cortex-imprint`, `cortex-crystallize`, `cortex-prune` skills.

- `fix:` — 2026-07-04 — `wiki/reference/` still existed as a separate physical directory even though its `type:` had already been unified to `concept` since 2026-06-30 (see that date's entry below). The exception that preserved the directory "for navigation" (documented in `skills/cortex-assimilate/SKILL.md`) was removed: the remaining 11 files were moved with `git mv` to `wiki/concepts/`, the `wiki/reference/` folder was deleted, and all `[[wiki/reference/*]]` wikilinks were updated to `[[wiki/concepts/*]]` in `wiki/index.md` (`## Reference` section merged into `## Concepts`), `wiki/projects/cortex-forge.md`, `wiki/concepts/agent-hook-compatibility.md`, `wiki/concepts/skill-dependency-graph.md`, and `docs/hot-cache-protocol.md`. `AGENTS.md`, `README.md`, and `skills/cortex-assimilate/SKILL.md` no longer mention `wiki/reference/` as a separate path or type.

- `protocol:` The "Resolve vault" block lived nearly identically in 5 skills (`cortex-assimilate`, `cortex-crystallize`, `cortex-imprint`, `cortex-prune`, `cortex-recall`), already with real drift between copies. Extracted to `references/VAULT-RESOLUTION.md`, co-located across the 5 skills following the same pattern as `LOCALE-RESOLUTION.md`; each `SKILL.md` keeps only the specific logic that isn't part of the shared core (what to do with remaining arguments in each skill, and the extra `/cortex-forge-setup` prompt in `cortex-crystallize`). `bin/check-skill-sync.sh` extended to watch for drift between the 5 copies.

- `fix:` `cortex-crystallize`'s Zone 2 (History) reproduced the full template inline in `SKILL.md`, duplicating — and already diverging from — the same template in `references/MEMORY-FORMAT.md` (the `SKILL.md` copy had `#### Imprint candidate` and the timestamp comment; the `MEMORY-FORMAT.md` one had neither). Fixed: `SKILL.md` now only references `MEMORY-FORMAT.md`, same as Zone 1 already did; `MEMORY-FORMAT.md` gains the two missing fields, becoming the single source of truth.
- `fix:` The "Trigger types" section of `MEMORY-FORMAT.md` documented `PreCompact`/`SessionEnd` — Claude Code hook event names that no longer exist since all agent lifecycle hooks were removed. Reframed agnostically: `Mid-session` (the user wants a checkpoint but the conversation continues) vs. `Handoff` (a real close, no return) — inferred from the user's invocation intent, not from a specific agent's hook name.

- `feat:` `cortex-crystallize` gains two lifecycle steps the original design (2026-06-28) specified but never implemented — confirmed by reviewing the original commit and transcript: the plan explicitly said "new step: update PRAXIS.md with conventions/workarounds learned this session," but the actual `Edit` only applied the pruning step (5b), never a write step.
  - **Step 6a — write to PRAXIS.md**: a deliberate judgment call, not an automatic log entry. The gate between `## Permanent` and `## Working context` is about **confidence, not category** — explicitly confirmed by the user, or repeated in a later session → `## Permanent` (with explicit promotion, never duplicated in both zones); a single-session observation → `## Working context` (pruned after 30 days if not reconfirmed).
  - **Step 5c — rotating History to `CONSOLIDATED.md`**: a three-file model clarified by the user — `MEMORY.md` is working memory (what's active now), `CONSOLIDATED.md` is episodic memory (what happened before, never auto-read, only on demand), `PRAXIS.md` is semantic memory (how to operate). `CONSOLIDATED.md` didn't exist at all — not on disk, not in any step — despite being in the original plan and mentioned in `wiki/projects/cortex-forge.md`; `MEMORY.md`'s `## History` grew unbounded (25.8K in this vault). Entries older than 30 days now move verbatim to `CONSOLIDATED.md`, preserving chronological order. `AGENTS.md` documents the new layer as on-demand, not part of the mandatory session-start read protocol.
- `fix:` `cortex-crystallize` step 1a (invoking-agent detection) hardcoded a fixed list of agents (`CLAUDECODE`, `COMMANDCODE`, `AGY`, `CODEX`) — contradicting the agent-agnostic principle. Replaced with self-knowledge as the primary method, with `env` only as generic corroboration with no fixed list.
- `fix:` `cortex-crystallize` step 3 (vault detection) required a `skills/` directory inside the vault — misaligned with `cortex-forge-setup`, which never requires it (skills install globally, not per-vault). Fixed to the same two criteria `cortex-forge-setup` uses (`wiki/`, `AGENTS.md`). Verified no other skill had the same misaligned criterion.
- `protocol:` Non-executable reference documentation moved to `references/` in each skill (`EMBEDDING-SETUP.md` in `cortex-forge-setup`; `MEMORY-FORMAT.md` and `PRAXIS-FORMAT.md` in `cortex-crystallize`), following agentskills.io/specification#file-references's distinction between `scripts/` (executables), `references/` (documentation the agent reads on demand), and `assets/` (static templates/data) — no `assets/` candidates identified in the current suite.
- `fix:` `LOCALE-RESOLUTION.md` was never actually co-located — it lived in `skills/` (one level above each individual skill), so `npx skills add --skill X` never installed it anywhere (confirmed: absent from `~/.agents/skills/`). The existing CI didn't catch it because `colocated-script-exists` only checks `.sh`/`.py` extensions. Now duplicated in `references/LOCALE-RESOLUTION.md` of the 3 skills that actually write user-locale prose to the vault (`cortex-assimilate`, `cortex-crystallize`, `cortex-imprint`); `bin/check-skill-sync.sh` extended to watch for drift between the 3 copies. The reference was **removed** (not moved) from `cortex-forge-setup` and `cortex-recall` — neither generates vault-locale prose (setup only persists the value as a config field; recall never reuses the value after reading it, and its only output is the conversation itself, already in the current language) — and from `cortex-prune`, which only writes a short, structured line to `log.md` with no free prose. Added a line to `LOCALE-RESOLUTION.md` itself explaining why it matters: content written today is read by a different agent, in a different session, potentially in a different language, later.
- `feat:` `cortex-forge-setup` now actually resolves a new vault's locale instead of just passively reading it. New step 3: detects the current conversation's language, proposes it as the default, and asks for confirmation (or an alternative ISO 639-1 code) before writing `locale:` to `config.yml` (the documented schema moves from flat `vaults: {name: path}` to nested `vaults: {name: {path, locale}}` — matching the format the file already used in production, never correctly documented until now). The `## Vault identity` stub added to `AGENTS.md` (step 8) is now written in the chosen language — field labels and comment prompts translated, while the `## Vault identity` heading and the ISO code itself stay fixed (the structural anchors the other skills look for literally). Maintenance menu gains option 8, "Change locale", for already-registered vaults.
- `protocol:` Co-located scripts moved into a `scripts/` subdirectory in each skill (`cortex-assimilate`, `cortex-forge-setup`, `cortex-recall`, `cortex-prune`), following the convention documented at agentskills.io/skill-creation/using-scripts. Previously they lived directly at the skill root next to `SKILL.md`; now: `scripts/cortex-index.py`, `scripts/embeddings.py`, `scripts/cortex-sanitize.sh`, `scripts/cortex-search.py`, `scripts/cortex-prune.sh`, `scripts/cortex-validate-schema.sh`, `scripts/cortex-reindex-post-commit.sh`. The "sibling file" resolution (`_resolve_embeddings_dir()`, `dirname "$0"`) keeps working with no code changes since the scripts moved together, as a group, into `scripts/`. `bin/check-skill-sync.sh` updated: `colocated-script-exists` now accepts references with or without the `scripts/` prefix and looks in both locations; `duplicated-script-sync` compares the copies inside `scripts/`. Each of the 4 affected `SKILL.md` files gains a standardized `## Available scripts` section (placed right after the intro paragraph, before `## Steps`/`## Sub-tasks`), listing each co-located script with its purpose and the step that invokes it — per agentskills.io, the recommended pattern so the agent knows what's available before reading the full steps.
- `protocol:` Removed `cortex-forge-setup`'s step 7 (installing a TASTE rule in `.commandcode/taste/`) and `TASTE-FORMAT.md`. It was the wizard's only point that hardcoded agent-specific logic (CommandCode) — directly contradicting the agent-agnostic principle just applied when removing the tarball and the Claude Code/Antigravity symlinks. Unlike those symlinks (purely redundant), this was a real feature with no agent-agnostic equivalent available; chose to remove it from the core rather than keep it as a documented exception or move it to an optional wiki snippet. A CommandCode user who wants this behavior can write their own TASTE rule pointing at `AGENTS.md` manually.

- `protocol:` Removed the tarball/curl-installer (`install.sh`, the build step in `.github/workflows/release.yml`) as a distribution channel. With `npx skills add` (skills.sh) already working as a complete, agent-agnostic installer — with no `~/.cortex-forge/` runtime required — keeping the tarball in parallel was an extra dependency to maintain and inferior in capability (doesn't detect installed agents, doesn't resolve per-agent symlinks, requires manual versioning). `cortex-forge-setup/SKILL.md` collapses its old steps 4-pre/4/5/5a (which installed hardcoded symlinks for Claude Code and Antigravity) into a single step 4 that just verifies the 6 skills are present in `~/.agents/skills/` and, if any are missing, points to `npx skills add itsmistermoon/cortex-forge --all -g -y` (or `-a <agent>` per the official supported-agents table) — never re-implementing install logic. `README.md` rewrites "## Setup" so `npx skills add` is the sole install method (the git clone remains only as a way to get the vault template) and "## Platform compatibility" removes the hardcoded agent table in favor of a link to skills.sh's official table. `cortex-index.py` and `cortex-search.py` drop the legacy fallback to `config.yml` for resolving `embeddings.py`, no longer needed without the tarball runtime.
- `protocol:` `cortex-forge-setup` gains a real step 6 in the new-vault wizard — offering semantic search directly, instead of it being reachable only through the maintenance menu (which a brand-new vault, by definition, hasn't seen yet) or buried behind opting into the post-commit reindex hook first. The offer checks dependencies _before_ asking: Ollama running + model pulled → one-line confirm; Ollama running, model not pulled → one-line "download + initialize?"; nothing available → full menu with tradeoffs (Ollama / mlx-embeddings / sentence-transformers / skip). `EMBEDDING-SETUP.md` rewritten to detect Ollama first (previously it only checked Python libraries, despite `embeddings.py` itself prioritizing Ollama) and to hand back the tailored wording instead of always showing the full menu. Maintenance menu option 3 and step 6c's fallback both now point at this same procedure instead of duplicating it. Final setup summary reports semantic search status either way.
- `protocol:` Eliminated all vault-local code execution. `cortex-recall` was flagged CRITICAL by the same Snyk audit (`E006: Malicious code pattern detected` — "executes arbitrary Python from vault paths ... permits RCE and covert data exfiltration/backdoors if a vault or its scripts are malicious"), because it ran `{vault}/.cortex/db/cortex-search.py` — a script `cortex-forge-setup` had copied into the vault. `cortex-assimilate`'s reindex step (7) had the identical pattern with `cortex-index.py`, unreported by the scanner but the same risk category. Fix: `cortex-search.py`+`embeddings.py` moved into `skills/cortex-recall/`; `cortex-index.py`+`embeddings.py` duplicated into `skills/cortex-assimilate/`; `cortex-forge-setup` no longer copies any of these into `{vault}/.cortex/db/` at all — it runs its own co-located `cortex-index.py` directly for setup, and stages `cortex-index.py`+`embeddings.py` at `~/.cortex-forge/bin/` only for the post-commit git hook (which structurally can't resolve "co-located with caller"). `{vault}/.cortex/db/` now holds only `vault.db` and `config.json` — no vault, however malicious, can plant code that any skill will execute. The intentional duplication (embeddings.py now in 3 skills, cortex-index.py in 2) is guarded by a new `bin/check-skill-sync.sh` invariant (`duplicated-script-sync`) that fails CI if the copies ever diverge — the alternative (a shared `bin/`) was rejected because it's exactly what broke independent `npx skills add --skill X` installs in the first place.
- `fix:` `cortex-sanitize.sh` — added credential detection (API keys `sk-*`, `Bearer *` tokens, `ghp_*`, `?token=*`, AWS `AKIA*` keys, basic-auth `user:pass` in URLs/flags) with **mandatory in-place redaction** — unlike other finding types, credentials are never just reported: the script rewrites the file itself before `cortex-assimilate` ever sees the JSON output, so there's no path (including explicit user insistence) to save the real value to `.raw/`. Found via a Snyk security audit on [skills.sh](https://www.skills.sh/) (`W007: Insecure credential handling`, risk 0.80) — the sanitize step previously only reported injection/exfiltration vectors and let the user override with "proceed anyway." `cortex-assimilate/SKILL.md` step 4a and Rules updated to match; the redaction logic mirrors the pattern already used (text-only, no script) in `cortex-crystallize/SKILL.md`. Two bugs caught while testing the fix: `sk-` pattern didn't allow hyphens (real keys look like `sk-ant-api03-...`), and patterns starting with `-` (`--password`, `-u user:pass`) were silently swallowed as flags by `rg`/`sed` instead of being treated as the search pattern — fixed by passing `--` before the pattern.
- `fix:` `cortex-forge-setup` step 3b (sync from upstream) — `templates/*.md` updates now require one batch confirmation before writing anything, instead of overwriting silently whenever local content differs from upstream. Found via the same Snyk audit (`W012: Unverifiable external dependency`, risk 0.85 for this skill) — a compromised `upstream:` (or a fork pointed at via config) could previously modify local skill templates with no visibility to the user. The confirmation batches all pending file changes into one list + one yes/no, matching the pattern the adjacent "Deletions" sub-step already used.

## [0.6.0] — Hookless Protocol & skills.sh Compatibility — 2026-07-03

Removes agent lifecycle hooks entirely and makes cortex-forge installable via [skills.sh](https://www.skills.sh/) (`npx skills add itsmistermoon/cortex-forge`), independent of the tarball/curl installer. Support for lifecycle hooks was too uneven across Claude Code, Codex, Antigravity, and CommandCode to build the suite on top of them; `AGENTS.md` now mandates one identical manual protocol on every agent instead. A follow-up fail-loud audit closed every silent-failure path this change touched — including a same-day regression the audit caught before it caused real damage.

- `protocol:` Agent lifecycle hooks (`SessionStart`, `PreCompact`, `SessionEnd`, `Stop`, `PreToolUse`) removed entirely — support was too uneven across Claude Code, Codex, Antigravity, and CommandCode to build the suite on top of it. `AGENTS.md` now mandates reading `.cortex/MEMORY.md` before the first response and invoking `/cortex-crystallize` manually, identically on every agent. `cortex-forge-setup` and `install.sh` no longer install any hook symlinks or merge `settings.json`/`hooks.json`. `bin/hooks/` reduced to the one script that remains — `cortex-reindex-post-commit.sh`, a git hook, not an agent lifecycle hook. See `wiki/concepts/agent-hook-compatibility.md` for the per-agent findings that drove the decision.
- `protocol:` `cortex-crystallize` — imprint-candidate detection moved from the (now-removed) SessionStart hook chain to a manual check: `AGENTS.md` step 6 instructs the agent to inspect the latest `## History` entry for `#### Imprint candidate` when reading `.cortex/MEMORY.md`.
- `protocol:` Scripts co-located with the skill that uses them (`cortex-prune.sh`, `cortex-sanitize.sh`, `cortex-validate-schema.sh`, `cortex-index.py`, `cortex-search.py`, `embeddings.py`, `cortex-reindex-post-commit.sh`) instead of living in a shared `bin/` that only the source repo ever had — so `npx skills add itsmistermoon/cortex-forge --skill X` installs a fully self-contained, working skill without depending on `~/.cortex-forge/` (the tarball runtime) at all. `~/.cortex-forge/bin/` is now only a runtime cache, populated at setup time, used exclusively by the two post-commit git hooks (which need a fixed absolute path since they run outside any agent session). `install.sh` kept working in parallel, not deprecated. Fixed two latent path bugs found in the process: `cortex-reindex-post-commit.sh` pointed at a nonexistent `{vault}/bin/cortex-index.py`, and `cortex-assimilate` step 7 pointed at `.cortex/cortex-index.py` instead of `.cortex/db/cortex-index.py`.
- `protocol:` `cortex-forge-setup` step 4 gains a pre-check (4-pre): before symlinking skills from `~/.cortex-forge/skills/` (the tarball runtime), verify all 6 skills are actually present there. If `~/.cortex-forge/skills/` is missing or incomplete, tell the user instead of silently creating broken symlinks — recommend `npx skills add itsmistermoon/cortex-forge --all -g -y` (doesn't depend on the tarball runtime at all) as the primary fix, with re-running the curl installer as the alternative.
- `fix:` Fail-loud audit across every script (`cortex-prune.sh`, `cortex-validate-schema.sh`, `cortex-sanitize.sh`, `embeddings.py`, `cortex-reindex-post-commit.sh`, `install.sh`, `cortex-embed.sh`, `check-skill-sync.sh`) — closes silent-failure paths found by reviewing each for unbounded waits, unchecked `mktemp`, and broken path references:
  - **Regression caught same-day:** the script co-location above moved `cortex-prune.sh` to `skills/cortex-prune/` but left `cortex-validate-schema.sh` behind in `bin/`, silently disabling schema-drift checks for every install. Co-located now; `check-skill-sync.sh` gained a new invariant (`colocated-script-exists`, later hardened to only match backtick-quoted filenames after it flagged a false positive on its own prose) that fails CI if any skill claims a co-located script that isn't actually present, specifically to catch this class of bug before it ships again.
  - `embeddings.py`: the real Ollama embed call had no timeout (only the 3s backend-detection ping did) — a stalled Ollama server could hang indexing/search indefinitely with no error. Added a 30s timeout with a clear message.
  - `cortex-reindex-post-commit.sh`: wrapped the backgrounded reindex in `timeout 300` (falls back to `gtimeout`, or runs unwrapped if neither exists — macOS ships no `timeout` by default) so a hang doesn't accumulate orphan processes across commits; timeouts are now distinguished from other errors in the log.
  - `cortex-prune.sh`: `mktemp` failures now abort loudly instead of writing to an empty path; the final exit-code computation no longer silently reports "no findings" (exit 0) if the findings file becomes unreadable mid-run.
  - `cortex-sanitize.sh`: added a `jq` availability check alongside the existing `rg` check — previously, a missing `jq` produced empty/invalid JSON output that `cortex-assimilate` could misread as "no findings."
  - `install.sh`: added `--max-time` to all `curl` calls (previously unbounded); `git pull --ff-only` against a local dev forge checkout now checks for uncommitted changes first and fails with an actionable message instead of a cryptic git error.
  - **Atomicity (follow-up pass):** `cortex-prune.sh`'s `vault-report.json` and `install.sh`'s `config.yml` writes went directly to the final path — a kill/crash mid-write could leave `vault-report.json` truncated, or worse, zero out `config.yml` (losing every registered vault). Both now write to a temp file in the same directory and `mv` atomically into place. Verified with a simulated config.yml update (existing entries survive) and a vault path containing spaces.
  - **Investigated, not changed:** paths with spaces (tested clean across `cortex-prune.sh`, `cortex-index.py`, `cortex-sanitize.sh`, `cortex-embed.sh`'s hand-rolled YAML lookup); sqlite concurrent-write handling (Python's 5s default `busy_timeout` plus a visible traceback on failure is adequate for this single-user usage pattern); unbounded `.git/cortex-*.log` growth (8KB after months of real use — not a practical risk). Manual YAML parsing in 4 scripts remains fragile against non-standard `config.yml` formatting, but adopting a real parser was judged not worth the added dependency (no native YAML in Python's stdlib, no clean option from bash) given `config.yml`'s schema is small and machine-generated.
- `protocol:` Scripts that skills invoke (`cortex-prune.sh`, `cortex-sanitize.sh`, `cortex-index.py`, `cortex-search.py`, `embeddings.py`, `cortex-reindex-post-commit.sh`) moved out of `bin/` to be co-located inside the `skills/<name>/` directory that uses them — so `npx skills add itsmistermoon/cortex-forge --skill X` (skills.sh) installs a fully functional skill without needing `~/.cortex-forge/` (the tarball runtime). `~/.cortex-forge/bin/` is now only a runtime cache, populated at setup time, used exclusively by the two post-commit git hooks (which need a fixed absolute path since they run outside any agent session). `install.sh` updated to match and kept working in parallel — not deprecated. Fixed two latent path bugs found in the process: `cortex-reindex-post-commit.sh` pointed at a nonexistent `{vault}/bin/cortex-index.py`, and `cortex-assimilate` step 7 pointed at `.cortex/cortex-index.py` instead of `.cortex/db/cortex-index.py`.

## [0.5.0] — Backward Enrichment, Drift Detection & Skill Quality Hardening — 2026-07-01

- `fix:` `27d1164` `cortex-prune` Layer 2: hard cap (20 pairs, 20 sources); replace "spawn subagents" language with always-inline evaluation.
- `fix:` `b613627` `bin/cortex-prune.sh`: `sources:` YAML frontmatter now counts as a valid reference — pages linked via `sources:` are no longer reported as orphans.
- `protocol:` `4273eee` Skill suite quality audit: no-op audit criteria made concrete and verifiable across all 6 skills; `cortex-crystallize` PRAXIS-FORMAT updated.
- `schema:` `43605e6` Vault taxonomy consolidated to 4 canonical types: `source`, `concept`, `entity`, `project`. Type `reference` deprecated; existing pages migrated to `concept`.
- `feat:` `3c3d63e` `bin/cortex-validate-schema.sh`: support for `type: series` in `wiki/projects/`.
- `protocol:` `99dccc5` Locale resolution extracted to `skills/LOCALE-RESOLUTION.md` — single source of truth replacing 5 inline duplicate blocks. `cortex-crystallize` description enriched with session-close trigger phrases.
- `protocol:` `d3f6b2e` Writing-great-skills audit: `cortex-assimilate` step 5 gains checkable completion criterion; `cortex-imprint` gains `disable-model-invocation: true`; `cortex-forge-setup` step 6d extracted to `EMBEDDING-SETUP.md` via context pointer; duplication and sediment removed across 5 skills.
- `protocol:` `7c8afe5` `cortex-assimilate` gains Step 9 — Backward enrichment: scan existing wiki pages for tag overlap after ingestion; propose additions per candidate with confirmation required.
- `protocol:` `7c8afe5` `cortex-prune` gains Layer 3 — Drift detection: compare `.raw/` mtime against `updated:` in `wiki/sources/`; MEDIUM finding when `.raw/` is newer.
- `schema:` `7c8afe5` Slash-tag convention (`project/subtopic`) adopted in all vault `AGENTS.md` files and templates; applied retroactively to 11 cortex-forge wiki pages.

## [0.4.0] — Protocol Hardening & Autonomous Imprint — 2026-06-29

- `breaking:` `.hot/` → `.cortex/` consolidation. All mutable state (`MEMORY.md`, `PRAXIS.md`, `imprint-draft.md`, `db/`) now under `.cortex/`. Existing vaults: rename `.hot/` to `.cortex/` or re-run `/cortex-forge-setup`.
- `protocol:` `cortex-imprint` autonomous mode (`imprint_triage: auto`): `bin/hooks/cortex-imprint-auto.sh` runs at SessionStart (background), reads `.cortex/imprint-draft.md` + transcript, calls `claude -p` (Haiku), writes wiki page, updates index and log, removes draft. Never blocks session start.
- `protocol:` Hook distribution architecture: `bin/hooks/` (git-tracked source) → `~/.cortex-forge/bin/hooks/` (runtime) → symlinks in `~/.claude/`, `~/.gemini/`, `~/.codex/`. `/cortex-forge-setup update` propagates all changes.
- `protocol:` `.cortex/PRAXIS.md` — permanent agent context split from `MEMORY.md`. `CODEX.md` absorbed into `AGENTS.md` under `## Vault identity`. `CONSOLIDATED.md` fallback added for history archive.
- `feat:` Multi-vault: `~/.cortex-forge/config.yml` with `vaults: {name: path}` + `default:`. Vault resolved by CWD then default. `/cortex-forge-setup` registers/deregisters by CWD. Legacy `vault:` key supported.
- `feat:` Stale cache detection: `hot_cache_stale_days:` in `config.yml`; `cortex-reactivate.sh` injects warning at session start if `.cortex/MEMORY.md` exceeds threshold.
- `protocol:` Context fencing in `cortex-imprint`: source hierarchy session > `.raw/` > `wiki/`. `raw:` field added to provenance.
- `feat:` `behavior:` frontmatter tags on all skills: `#ingest`, `#synthesize`, `#recall`, `#prune`, `#snapshot`, `#configure`.
- `feat:` `cortex-prune.sh` link-count scan — orphan page detection; `orphan_pages` added to `vault-report.json`.
- `fix:` `cortex-recall` blocks Explore as bypass; accounts for optional embedding index; fallback behavior clarified.
- `feat:` Semantic search backend stable: `bin/embeddings.py` (Ollama → mlx → sentence-transformers), `bin/cortex-index.py`, `bin/cortex-search.py`. Auto-reindex on assimilate + post-commit hook.
- `fix:` Antigravity crystallize hook removed — `agy -p` deadlocks from hook; no `/exit` trigger. Crystallize in Antigravity is manual-only.
- `fix:` Multiple path corrections for `cortex-sanitize.sh`, `cortex-prune` executable, and hooks after `.cortex/` migration.

## [0.3.0] — Multi-vault, cortex-prune global — 2026-06-15

- `schema:` `AGENTS.md` gains YAML frontmatter with `schema_version: "0.3"`. All six templates (`concept`, `entity`, `project`, `reference`, `source`, `wiki-page`) gain `schema_version: "0.3"` in their frontmatter. Pages with an older `schema_version` (or none) may be missing fields introduced after their creation — detectable by `cortex-prune`.
- `protocol:` Pipeline imprint implemented end-to-end: `cortex-crystallize-claude.sh` detects durable insights via Haiku and emits `#### Imprint candidate` with `— transcript: <path>` in History. `cortex-reactivate.sh` (SessionStart) detects the candidate in the most recent History entry, checks 30-day expiry, writes `.hot/imprint-draft.md`, and injects a nudge. Toggle `imprint_triage: off | suggest | auto` in `~/.cortex-forge/config.yml` (global or per-vault; backwards compat `true`→`suggest`, `false`→`off`; default `suggest`). `cortex-imprint/SKILL.md` gains step 0: read and delete `.hot/imprint-draft.md` if present.
- `protocol:` History archival: `cortex-crystallize-claude.sh` moves History entries older than 30 days from `.hot/MEMORY.md` to `.hot/CONSOLIDATED.md` (append-only, not injected at session start) on every crystallize run.
- `feat:` `bin/hooks/cortex-recall-nudge.sh` — PreToolUse nudge (Bash matcher, Claude Code only, v1) that injects a `/cortex-recall` reminder when a search command targets `wiki/` or `.raw/` inside a registered vault. Once per session, fail-open on every branch, inert outside vaults. Installed via `cortex-forge-setup` step 6a into the vault's `.claude/settings.local.json`. Ports to other agents gated on the AGENT-LOG behavior experiment (backlog #2, Item 1).
- `protocol:` `cortex-forge-setup` gains steps 6a (recall nudge, Claude Code only) and 6b (opt-in post-commit hook refreshing `vault-report.json`, marked block, backgrounded, summary line to `.git/cortex-prune.log`) — backlog #2, Item 2.
- `protocol:` `skills/cortex-crystallize/MEMORY-FORMAT.md` and `SKILL.md` gain `#### Attempted and failed` section in the History template — records approaches that failed, with evidence, to prevent retrying dead ends (Fase 2.5, Item 1).
- `feat:` `bin/cortex-sanitize.sh` — sanitization scan for injection/exfiltration vectors (invisible Unicode, HTML comments, embedded base64, egress commands, `ANTHROPIC_BASE_URL`). `skills/cortex-assimilate/SKILL.md` gains step 4a: before saving to `.raw/`, scans content and reports findings to the user. Findings don't block — they inform (Fase 2.5, Item 2).
- `protocol:` `cortex-recall` output contract appends `[confidence: {value}]` to every citation — `unset` and `read-error` are flagged as findings; `medium`/`low` are valid states. `AGENTS.md` Recall compliance criterion updated to match.
- `schema:` `wiki/meta/vault-report.json` canonical minimal schema defined in `cortex-prune` step 4a (`generated` + `health.dead_links` / `health.raw_without_source_page` / `health.missing_confidence`). Written on every prune run, read at session start per `AGENTS.md` startup step 3, gitignored.
- `protocol:` `AGENTS.md` session startup sequence gains step 3: read `vault-report.json` and surface non-empty `dead_links` / `raw_without_source_page`.
- `feat:` `docs/obsidian-visualization.md` — `wiki/` documented as a native Obsidian vault; linked from `README.md` § Visualization.
- `docs:` platform compatibility table and commit convention added to `README.md`.
- `protocol:` `vault-report.json` is now written directly by `bin/cortex-prune.sh` (Layer 1) — single writer, structured output, no stdout parsing. `cortex-prune` step 4a verifies the file instead of writing it.
- `schema:` `templates/source.md` frontmatter aligned with the convention every existing source page uses and `bin/cortex-prune.sh` verifies: `source:` / `slug:` / `section:` / `fetched:` / `raw:` replace `source_url:` / `source_date:` / `source_author:` / `created:` / `updated:`. `raw:` is the page's context pointer to its `.raw/` primary.

## [0.2.0] — CODEX.md, Reference taxonomy, AI Coding Dictionary, handoff improvements — 2026-06-09

- `protocol:` `CODEX.md` — new vault identity file (Mission, Owner, Domains, Vocabulary, Out of scope), read at session start after `.hot/MEMORY.md`. All skills check it for relevance and vocabulary decisions.
- `protocol:` Parametric knowledge explicitly disqualified in `cortex-recall` — vault is always source of truth for vault topics, regardless of training knowledge.
- `schema:` `wiki/reference/` — fifth wiki type for lookup tables, wire formats, and cheat sheets (`templates/reference.md`). Distinct from Concept: scannable, not explanatory.
- `fix:` `cortex-crystallize` PreCompact now uses `claude -p` — previously generated a raw list of file paths with no description. Both PreCompact and SessionEnd synthesize descriptive bullets. Prompts distinguish compaction (mid-session) from handoff (no return path).
- `protocol:` `MEMORY-FORMAT.md` gains trigger table (PreCompact vs SessionEnd), optional `### Suggested skills` section, and `next: <focus>` argument to orient snapshots.
- `protocol:` Templates co-located with their skills: `MEMORY-FORMAT.md`, `CODEX-FORMAT.md`, `TASTE-FORMAT.md`.
- `fix:` `.hot/MEMORY.md` fixed filename — removed project-name detection; one file per repo.
- `protocol:` `AGENT-LOG.md` — append-only session log with minimal template and drift-prevention rules.
- `schema:` `AGENTS.md` architecture table labels `.raw/` as primary sources and `wiki/` as secondary sources, with conflict resolution rule.
- `knowledge:` AI Coding Dictionary ingested (68 entries). New concepts: `parametric-knowledge`, `contextual-knowledge`, `memory-system`, `handoff-artifact`, `smart-zone`.

## [0.1.0] — First usable release — 2026-06-08

- `feat:` 6 skills: `cortex-crystallize`, `cortex-assimilate`, `cortex-recall`, `cortex-imprint`, `cortex-prune`, `cortex-forge-setup`.
- `protocol:` `AGENTS.md` with MANDATORY protocols and verifiable compliance criteria for recall, assimilation, and crystallize.
- `protocol:` Parametric knowledge disqualified as source for vault topics — epistemological rule, not workflow instruction.
- `feat:` Multi-vault support via `~/.cortex-forge/config.yml`. Vault selectable as explicit argument: `/cortex-recall second-brain <query>`.
- `schema:` `CODEX.md` — vault context file (Mission, Owner, Domains, Vocabulary, Out of scope).
- `schema:` `wiki/reference/` — taxonomy type for lookup tables and wire formats.
- `feat:` SPA detection fallback in `cortex-assimilate` — JS bundle inspection + static asset reconstruction.
- `protocol:` Invoke messages per skill (`Crystallizing memory...`, `Recalling memory...`, etc.).
- `feat:` Agent compatibility: Claude Code (full hook support), Codex (SessionStart confirmed), Antigravity (hooks configured), CommandCode (AGENTS.md MANDATORY confirmed).
