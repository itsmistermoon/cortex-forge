# cortex-forge-setup — skill changelog

Change history for `skills/cortex-forge-setup/SKILL.md`, moved out of the skill file to keep it lean. Newest first.

---

- 2026-07-06 [Claude Code]: POST-COMMIT-HOOKS.md's "Uninstall (deregister path)" line had no caller anywhere in the skill — grepped the whole skill directory to confirm. Wired it into maintenance menu option 9 ("Remove this vault"): now offers to uninstall any installed post-commit hooks before removing the vault's config entry, closing the gap where hooks would otherwise run forever, orphaned, after the vault they belonged to was deregistered. Updated POST-COMMIT-HOOKS.md's intro to name option 9 as the uninstall entry point. Also removed a leftover "(co-located with this skill)" no-op in the reindex hook's copy step — missed in the earlier suite-wide cleanup because it lived in a reference file, not SKILL.md itself
- 2026-07-06 [Claude Code]: EMBEDDING-SETUP.md — the Ollama detection couldn't distinguish "never installed" from "installed but not currently running" (both looked like `not-running` to the curl check), so the flow fell through to the "Full menu" with "[1] Ollama — installs Ollama..." even when the user already had it, just stopped. Added a `command -v ollama` sub-check under `not-running` and a new "Installed but not running" wording branch that asks the user to start it instead of jumping to install instructions. The existing "Installation, by choice" section already anticipated this distinction in its own wording ("ask the user to confirm it's installed and running") without the detection ever actually producing it — now it does
- 2026-07-06 [Claude Code]: Removed the "AGENTS.md structural check" feature entirely (UPSTREAM-SYNC.md's own section, the Scope table's "structurally diffed" clause, and SKILL.md Output format's "AGENTS.md divergence noted if any"). Decision: this skill (and the suite generally) shouldn't assume a vault's AGENTS.md resembles the upstream template closely enough for a rigid `##`/`###` heading diff to stay meaningful — a vault's AGENTS.md is expected to diverge from the template over time as it's tailored, and skills need to work standalone regardless of how customized the calling vault's AGENTS.md is. `AGENTS.md` is now simply never touched by this sync, full stop — no read-and-compare either
- 2026-07-06 [Claude Code]: Fixed a real bug in UPSTREAM-SYNC.md's Procedure step 1 — the API call hardcoded `main` in the URL path (`.../git/trees/main?recursive=1`) despite Scope documenting `upstream_ref:` as a configurable ref (default `main`); the setting was never actually wired in, so a non-default ref was silently ignored. Now `.../git/trees/{upstream_ref}?recursive=1`
- 2026-07-06 [Claude Code]: UPSTREAM-SYNC.md — added real header labels ("Field | Value") to the ## Scope table, the only blank-header table in the whole suite (every other table — Sub-tasks, Detection criteria — has real labels); tightened the intro from "pulls infrastructure files" to "pulls updated templates" to match Scope's own "templates/*.md only", removing a vagueness that could read as broader than it is
- 2026-07-06 [Claude Code]: Trimmed ## Sub-tasks' "Runs" column to bare step references — the trailing descriptions ("sync infrastructure files...", "verify skills are installed...", etc.) just restated what each step's own title/body already says, and this table is never shown to the user, only read by the agent, which reads the actual step text anyway when it jumps there. A no-op by the whiteness test
- 2026-07-06 [Claude Code]: MAINTENANCE-MODE.md cleanup — removed a redundancy I'd introduced in the previous round (options 9's note and the closing "regardless of combination" line said the same thing two different ways); added a missing execution-order rule for option 9 (remove) when combined with other selections — run it last, since other operations may need the vault's entry to still exist; tightened the intro's "steps 3–6 behave differently" to name only the ones that actually do (3, 3a, 5) instead of implying all of them differ
- 2026-07-06 [Claude Code]: Generalized the flavor-line instruction's scope — it only ever said to translate that one opening line; extended to cover every prompt, question, menu, and confirmation this skill produces, with an explicit carve-out that persisted vault content still follows the vault's locale, not conversation language. Applied identically across all 6 skills — surfaced directly while reviewing this skill's MAINTENANCE-MODE.md, which had no translation instruction for its menu at all despite the menu being entirely about locale
- 2026-07-05 [Claude Code]: Final review round — independently cross-checked by Fable and Gemini (both given the 5 SKILL.md files + references/ for comparison), findings reconciled and verified before applying:
  - **Real bug (Fable, verified empirically):** references/EMBEDDING-SETUP.md's platform-detection line imported `mlx_lm` (a different, unrelated package — the actual runtime backend in embeddings.py imports `mlx_embeddings`) and had a broken `&&`/`||` chain that printed both "mlx" and "st" when the first check succeeded (confirmed by running the exact line). Rewrote as a proper if/elif/else checking the correct module.
  - **Real contradiction (Fable + independent read):** Rules' "Do not modify any vault files during setup — read only for validation" directly contradicted steps 3b (writes templates/), 5 (creates .cortex/db/vault.db), and 5a (writes git hooks). Narrowed to the actual invariant: never write to wiki/, .raw/, or AGENTS.md — everything else this skill writes is infrastructure, not vault content.
  - **Dead/confusing step (Fable + Gemini, independently, same finding):** step 5b was pure residue — nothing pointed to it (not Sub-tasks, not the maintenance menu, not EMBEDDING-SETUP.md's own entry-point list), and its wording was grammatically broken. Removed as a step; folded its one piece of real information (the mid-session re-trigger condition) into step 5's own text.
  - **Stale/false claims (Fable):** MAINTENANCE-MODE.md's sync option promised "templates and bin scripts" — UPSTREAM-SYNC.md's own scope table says templates only; corrected. Its semantic-search option said ".cortex/vault.db" — canonical path everywhere else is ".cortex/db/vault.db"; corrected.
  - **Genuine ambiguity (Fable):** Sub-tasks' `vaults` row said "Steps 2–3a", but step 2 can just as well route to the maintenance menu instead of continuing to 3a — the fixed range was only sometimes true. Reworded to describe step 2's actual branching instead of asserting a static range.
  - **Terminology ambiguity (Gemini, partially accepted):** the word "stop" is used with different scopes across these files — full-skill-abort in step 1, but "this procedure/operation is done" in UPSTREAM-SYNC.md step 4 and maintenance option 9. Confirmed by an independent reviewer misreading both as full aborts. Added explicit scoping words in both places rather than rewriting the actual control flow, which was already correct.
  - **Evaluated and rejected (Gemini):** reindex hook re-offering semantic search after a decline in step 5 is an intentional reconciliation of two different user answers, not a bug or loop — left as-is. The relative path `../cortex-prune/scripts/cortex-prune.sh` in POST-COMMIT-HOOKS.md is safe given the suite's confirmed sibling-install architecture (all 6 skills install together via `npx skills add`) and already fails gracefully if not found — left as-is.
  - **Resolved:** added the flavor line ("Start your response with the flavor line `Setting up vault...`, translated to..., Spanish: `Configurando vault...`"), matching all 5 sibling skills — confirmed not deliberate.
- 2026-07-05 [Claude Code]: Extracted step 7 ("Confirm result") to its own ## Output format section, matching cortex-assimilate/cortex-crystallize/cortex-recall's pattern — the other 5 skills keep the final-response contract separate from ## Steps; this skill had it as the last numbered step instead. ## Steps now ends at 6; updated the two cross-references (Sub-tasks' "end with step 7", MAINTENANCE-MODE.md's "show confirmation (step 7)") plus two now-stale "steps 3–7" ranges (step 2's own branching, MAINTENANCE-MODE.md's intro) that would have been silently wrong the moment step 7 stopped being a step
- 2026-07-05 [Claude Code]: Relettered 5b→5a and 5c→5b, closing the "5a never existed" gap now that the 5b/5c merge left only two lettered sub-steps under step 5. Propagated to every cross-reference (Available scripts ×2, Rules, MAINTENANCE-MODE.md ×2, POST-COMMIT-HOOKS.md, EMBEDDING-SETUP.md)
- 2026-07-05 [Claude Code]: Renamed step 1 from "Detect vault from CWD" to "Validate vault candidate" — clarifies it's a structural filesystem check (does CWD qualify to be registered?), distinct from step 2's registration check (is this path already an entry in ~/.cortex-forge/config.yml?). Considered reversing the order of steps 1/2 and reusing VAULT-RESOLUTION.md like the other 5 skills — kept both as-is: structure validation has to gate both downstream branches (new-vault wizard and maintenance mode) regardless of registration status, and VAULT-RESOLUTION.md's own fallback chain treats "nothing registered" as its terminal failure state (pointing here), the opposite of this skill's primary scenario — reusing it here would be circular
- 2026-07-05 [Claude Code]: Merged steps 5b (Post-commit prune) and 5c (Post-commit re-index) into a single "Post-commit hooks" step — both were opt-in, separate questions pointing to the exact same reference file with near-identical boilerplate; kept as two sub-bullets (prune, reindex) rather than merging their actual behavior, since reindex genuinely depends on semantic search being enabled and prune doesn't — that asymmetry stays visible via the sub-bullet note, not by keeping two step numbers. Renumbered the old 5d (Embedding dependency check) to 5c. Propagated the renumbering to every cross-reference: Available scripts (×2), Rules, references/MAINTENANCE-MODE.md (options 6/7 both now point to step 5b, disambiguated by "(prune)"/"(reindex)"), references/POST-COMMIT-HOOKS.md (intro line + both section headers, which no longer need per-section step labels since the intro states it once), references/EMBEDDING-SETUP.md (the fallback trigger point). Confirmed no historical trace of a "5a" ever existing — the 5, 5b, 5c, 5d gap predates this session and isn't something to fix by relettering
- 2026-07-05 [Claude Code]: Removed step 4's "-a <agent> instead of --all" flag guidance line
- 2026-07-05 [Claude Code]: Renamed references/MAINTENANCE-MENU.md → MAINTENANCE-MODE.md and moved the existing-vault branches out of steps 3 and 3a (added two entries ago) back into it. Rationale: those inline branches meant every new-vault run — the common case — had to read existing-vault-only text that never applies to it, the exact "unconditional read of conditional content" anti-pattern this session has been fixing elsewhere. Steps 3/3a/5 in ## Steps now describe only the new-vault flow again; MAINTENANCE-MODE.md is the single, genuinely-conditional place documenting how steps 3–7 differ when reached via the maintenance menu instead. Simplified step 5's title (dropped the "for an already-registered vault, use maintenance menu option 5 instead" parenthetical — fully redundant with MAINTENANCE-MODE.md's own mapping) and step 2's "If not registered" branch (dropped "asking each optional step interactively" — a no-op restating what every downstream step already says explicitly)
- 2026-07-05 [Claude Code]: Folded step 2b into step 2's own "If already registered" branch — once its content was just a one-line pointer to references/MAINTENANCE-MENU.md, it didn't need its own step number; the pointer now lives directly in the branch that triggers it
- 2026-07-05 [Claude Code]: Extracted step 2b (maintenance menu) to references/MAINTENANCE-MENU.md — it only loads when step 2 finds the vault already registered, never on a new-vault run, so this is genuinely conditional content (unlike VAULT-RESOLUTION.md-style references read on every invocation). Reordered its 9 options to ascending step order (1 Change locale [3,3a] → 2 Sync [3b] → 3 Stale-cache [3c] → 4 Check skills [4] → 5 Semantic search [5] → 6 Post-commit prune [5b] → 7 Post-commit reindex [5c] → 8 Set default [6] → 9 Remove vault [no step, moved last as the destructive action] — was scrambled: 4,3b,5,5b,5c,-,6,3&3a,3c). Propagated the renumbering to every cross-reference: SKILL.md step 5 ("option 3"→5), EMBEDDING-SETUP.md (×2, "option 3"→5, plus a pre-existing, unrelated stale reference to "step 6"/"step 6c" that don't exist — corrected to step 5/5d while already touching that line), POST-COMMIT-HOOKS.md (×2, "options 4/5"→6/7, "option 5"→7).
- 2026-07-05 [Claude Code]: Fixed steps 3 and 3a with a real existing-vault branch instead of just caveating the title (as step 5 does) — both are reused by the maintenance menu's "Change locale" option, but their bodies only described new-vault behavior. Step 3's prompt now shows the vault's current locale before asking, mirroring step 3c's already-established pattern, instead of asking as if for the first time. Step 3a's "add the vault entry" (with first-vault/default logic, irrelevant to a locale-only edit) now branches: new vault adds the full entry, existing vault updates only the `locale:` field on its current entry.
- 2026-07-05 [Claude Code]: Reordered ## Sub-tasks' table rows to ascending step order (vaults 2–3a → sync 3b → skills 4 → embeddings 5) — was listed in reverse (5, 4, 3b, 2–3a), harder to scan against the skill's actual step flow
- 2026-07-05 [Claude Code]: Standardized "Resolve context" opening (step 2's title/body no longer repeat, matching the pattern applied to the other 5 skills) and added the "Paths are relative to this skill's directory" convention line to ## Available scripts.
- 2026-07-05 [Claude Code]: Removed a quadruplication — "npx skills add / skills.sh is the sole installer, agent-agnostic, never hand-roll agent-specific logic" appeared in the intro, step 4 (twice), and ## Rules. Trimmed step 4 to just the operative check (present/missing skills, install command), kept the rule in the intro (context) and ## Rules (hard invariant) only.
- 2026-07-05 [Claude Code]: Removed the entire "## Memory model (manual, no agent lifecycle hooks)" section and its intro pointer — pure development-decision rationale (why Cortex Forge doesn't use SessionStart/PreCompact/SessionEnd/PreToolUse hooks), irrelevant to what this skill's steps actually do during setup. Trimmed the same rationale out of the "Post-commit git hooks" Rules bullet, keeping only the operative constraint (steps 5b/5c are the only hooks this skill installs).
- 2026-07-05 [Claude Code]: Removed 5 mentions of "(co-located with this skill)" (steps 3b, 5, 5b, 5c, 5d) — same no-op pattern fixed in the other 5 skills once "Available scripts" states the convention once.
- 2026-07-05 [Claude Code]: Removed two Rules bullets duplicating fully-specified steps: "Preserve all existing vault entries when writing config" (step 3a already says this, with more precision) and "Always ask for default when there are multiple vaults — never assume" (step 6 already has the complete conditional logic).
