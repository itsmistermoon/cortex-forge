---
"cortex-forge": patch
---

Aligned all six SKILL.md frontmatter blocks with the Agent Skills specification: removed the custom `behavior:` field, added `license: MIT`, declared `compatibility:` where scripts have system dependencies, shortened four descriptions to fit Claude.ai's 200-character limit, and fixed cortex-assimilate's template path to `{vault}/templates/{type}.md`.
