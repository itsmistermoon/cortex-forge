---
"cortex-forge": patch
---

Added `skills.sh.json` to group the 6 skills under "Cortex Forge" on the repo's skills.sh page (distinct from `plugin.json`'s `skills` array, which groups them for `npx skills ls -g`). Added an `AGENTS.md` rule requiring a check against the `skill-design-principles` checklist (moon-multivac vault) before opening a PR that adds a skill or changes an existing one significantly.
