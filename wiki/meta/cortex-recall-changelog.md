# cortex-recall — skill changelog

Change history for `skills/cortex-recall/SKILL.md`, moved out of the skill file to keep it lean. Newest first.

---

- 2026-07-05 [Claude Code]: Merged steps 3 (Read full pages) + 4 (Synthesize response) + 5 (Suggest ingestion) into a single "Answer" step — none had enough weight to stand alone; all three are sub-parts of the same act of producing the response, unlike steps 1/2 (setup vs. search), which are genuinely distinct phases. 5 steps → 3, no instruction dropped
- 2026-07-05 [Claude Code]: Standardized ## Output format to a numbered list matching cortex-assimilate/cortex-crystallize (was a flat bullet list); nested the 3 confidence-value sub-cases under item 1 as sub-bullets, since they're variants of the same citation requirement rather than independent items. Compared ## Constraints against the rest of the suite — this is now the only skill with a standalone Constraints section (cortex-imprint's was removed as pure duplication earlier this session), but kept it as-is: its 2 bullets are genuine hard invariants (no parametric knowledge, no bypassing the search mechanism), distinct in kind from Rules' 2 execution-behavior bullets, matching the Constraints-vs-Rules distinction documented in wiki/concepts/skill-design-principles.md — no internal duplication remained after the earlier triplication fix, so no further changes needed there
- 2026-07-05 [Claude Code]: Added bold titles to steps 3-5 (only 1-2 had them) for consistency with the rest of the suite, then reworded each body to not restate its own title (matching step 1's pattern) — "read the full page..." → "for any result where...", "synthesize a response..." → "with citations to...", "suggest sources to ingest..." → "point to /cortex-assimilate for..."
- 2026-07-05 [Claude Code]: Fixed a real triplication flagged by our own wiki/concepts/skill-design-principles.md ("Rules that repeats items already in Constraints"): the "reading wiki/index.md directly is not a bypass" clarification appeared in step 2, ## Constraints, and ## Rules simultaneously — kept it only in step 2 (where the fallback actually happens), removed the Rules bullet entirely (added nothing beyond what step 2 + Constraints already said). Also removed Rules' "Cite wiki pages, not parametric knowledge" — duplicated Constraints' own "Parametric knowledge is disqualified..." bullet. Standardized step 1's "Resolve vault" phrasing and Available scripts' intro line ("Paths are relative to this skill's directory") to match cortex-assimilate/cortex-imprint/cortex-crystallize/cortex-prune; dropped the no-op "(argument → CWD → default)" parenthetical and the redundant co-location explanation in step 2, both already covered by references/VAULT-RESOLUTION.md and the new Available scripts intro
- 2026-07-05 [Claude Code]: Synced this skill's `embeddings.py` copy with the fixes made while auditing cortex-assimilate's scripts (they were byte-identical duplicates, no prior drift) — adds `EmbeddingBackendError`, `DIMENSIONS` validation, and a shared `_ollama_post()` helper. Updated `cortex-search.py`'s single `emb.embed_query()` call to catch `EmbeddingBackendError` and exit the same way `embeddings.py` used to internally — no behavior change, just moved the exit point to the caller since embeddings.py no longer exits on its own for a per-call timeout. See cortex-assimilate's changelog for the full rationale

- 2026-07-05 [Claude Code]: Removed the AGENTS.md ## Vault identity dependency (section eliminated from the suite): dropped step 2 (Vocabulary/Domains) and renumbered
- 2026-07-05 [Claude Code]: Compressed the flavor-line instruction to a single sentence — removed the duplicated position instruction and the "literally" vs "translated" contradiction
- 2026-07-04 [Claude Code]: Centralized vault structure validation (`wiki/`+`AGENTS.md`) in `references/VAULT-RESOLUTION.md`, closing a gap where step 2 assumed `AGENTS.md` existed without validating it
- 2026-07-04 [Claude Code]: Reworded "Resolve vault" step intro to point directly at VAULT-RESOLUTION.md's decision flow, removing the vague closing phrase
- 2026-07-04 [Claude Code]: Extracted "Resolve vault" logic to shared `references/VAULT-RESOLUTION.md`, co-located across 5 skills (was duplicated inline with real drift between copies)
