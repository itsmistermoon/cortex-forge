# cortex-imprint — skill changelog

Change history for `skills/cortex-imprint/SKILL.md`, moved out of the skill file to keep it lean. Newest first.

---

- 2026-07-05 [Claude Code]: Removed the ## Valid types and paths section — its only non-redundant content (type → wiki/ path mapping) folded directly into step 4, where it's used; the "What was produced" descriptive column added nothing beyond what the type name already conveys, and full creation criteria for concept/entity/project already live in cortex-assimilate. Compressed ## Provenance's YAML code block to prose — the field names (sources:, confidence:, raw:) are already declared in the actual templates (templates/{type}.md), so only the unique value/format conventions and confidence criteria were kept. Evaluated and rejected moving confidence criteria into the templates themselves (unlike MEMORY-FORMAT.md, which only cortex-crystallize writes, templates/{type}.md is written by both cortex-assimilate and cortex-imprint, and the two already define different confidence rubrics for the same field — assimilate's is about the external source's nature, imprint's about the synthesis's nature; a single shared comment would force a false one-size-fits-all criterion)
- 2026-07-05 [Claude Code]: Added bold titles to all steps (only step 1 had one) for consistency with the rest of the suite; merged Propose (old 3) + Wait for confirmation (old 4) into one "Propose and confirm" step, and Add wikilinks (old 6) + Update index (old 7) + Log entry (old 8) into one "Link, index, and log" step — 8 steps → 5, no instruction dropped; step "Create the page" (old 5, now 4) now states the template path (`{vault}/templates/{type}.md`), missing before; fixed the "Valid types and paths" table's "Design decision for a project" row — Type was "page (ADR)", not matching the actual template file (`templates/project.md`, which has its own `## Key decisions` section) — now "project", with the create-vs-update distinction moved into step 4's own text; updated Provenance's "(step 5)" cross-reference to "(step 4)" after the renumbering
- 2026-07-05 [Claude Code]: Reworded step 1's vault/locale sentence to match cortex-crystallize's phrasing ("per VAULT-RESOLUTION.md, then its locale: per...") for consistency across the suite; dropped the separate bullet sub-item, folding the vault-name-match clause inline like crystallize does; avoided the "Resolve vault — resolve the vault" doubling that a literal copy would have introduced (crystallize's bold header says "Resolve context", a different word from its body's "Resolve the vault")
- 2026-07-05 [Claude Code]: Removed the ## Constraints section (one bullet, fully duplicated by ## Source hierarchy's circular-synthesis test and Rules' "Source fencing" bullet) — its own cross-reference was also wrong ("see circular synthesis test in Rules", but the test lives in ## Source hierarchy, not ## Rules); folded Source fencing's unique rationale ("amplifies drift instead of grounding knowledge") into the circular synthesis test itself, then removed the now-redundant Source fencing bullet from Rules; fixed step 2's "(see Rules)" pointer to correctly point at ## Source hierarchy; dropped two more Rules bullets that duplicated earlier, more complete ones ("No references to the conversation..." duplicated the self-contained criterion's deictic-reference clause; "update it instead of duplicating" duplicated the preceding rewrite-the-body bullet); clarified "Include the agent in the changelog" to "the page's own changelog" to disambiguate from step 8's wiki/meta/log.md entry; dropped the no-op "(argument → CWD → default)" from step 1, already covered by VAULT-RESOLUTION.md. SKILL.md: 81 → 75 lines
- 2026-07-05 [Claude Code]: Removed the AGENTS.md ## Vault identity dependency (section eliminated from the suite): dropped step 2 and renumbered — the Provenance "(step 5)" reference is accurate again; locale fallback no longer checks AGENTS.md
- 2026-07-05 [Claude Code]: Compressed the flavor-line instruction to a single sentence — removed the duplicated position instruction and the "literally" vs "translated" contradiction
- 2026-07-04 [Claude Code]: Centralized vault structure validation (`wiki/`+`AGENTS.md`) in `references/VAULT-RESOLUTION.md`, closing a gap where step 2 assumed `AGENTS.md` existed without validating it
- 2026-07-04 [Claude Code]: Reworded "Resolve vault" step intro to distinguish VAULT-RESOLUTION.md (decision flow) from LOCALE-RESOLUTION.md (fallback chain), removing the repeated closing phrase
- 2026-07-04 [Claude Code]: Extracted "Resolve vault" logic to shared `references/VAULT-RESOLUTION.md`, co-located across 5 skills (was duplicated inline with real drift between copies)
- 2026-07-04 [Claude Code]: Removed dead step 0 (pending draft check) — `.cortex/imprint-draft.md` was never created by any process; it was a vestige of a superseded design
- 2026-06-28 [Claude Code]: Context fencing — added source hierarchy section, circular synthesis test, source fencing rule, and `raw:` provenance field; updated step 2 (CODEX.md → AGENTS.md vault identity) and step 3 (references source hierarchy)
- 2026-06-24 [Claude Code]: Reformulated vague Rules into verifiable criteria (no-op audit — "durable page" → 4 testable conditions; "compiled truth" → explicit rewrite contract)
