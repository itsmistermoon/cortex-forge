# cortex-crystallize — skill changelog

Change history for `skills/cortex-crystallize/SKILL.md`, moved out of the skill file to keep it lean. Newest first.

---

- 2026-07-05 [Claude Code]: Removed step 5 ("If cross-vault mode: run cross-vault update (see section below)") — pure forward-pointer scaffolding now that ## Cross-vault update sits immediately after the Steps list and already self-gates with its own opening line; reworded that opening line from "When invoked from outside the vault" to "If cross-vault mode (step 1)" to trace the condition back to where it's actually decided instead of restating it independently
- 2026-07-05 [Claude Code]: Clarified that `{vault}` in Cross-vault update's step 1 refers to the vault resolved in ## Steps' step 1 — previously implicit (inferred by elimination, the only "vault" concept in the file), now explicit to avoid ambiguity with Cross-vault update's own internal 1-6 numbering
- 2026-07-05 [Claude Code]: Redesigned Cross-vault update's two blind questions into agent-proposed suggestions: "Knowledge applied" now scans the session for vault pages actually consulted (/cortex-recall citations, wiki pages read directly) and skips silently if none surfaced, instead of asking the user to recall this from memory; "Recurring issues" now first checks whether step 3's PRAXIS gate already surfaced a recurring failure pattern and proposes that, falling back to the open question only if PRAXIS found nothing — reuses detection work the skill already does instead of asking twice
- 2026-07-05 [Claude Code]: Moved the credential-redaction paragraph ("In #### Fragile context and any other section, omit tokens...") from a floating, unattached position between ## Steps and ## Cross-vault update into MEMORY-FORMAT.md, next to the other content-authoring rules it belongs with
- 2026-07-05 [Claude Code]: Dropped "Update `agent:` and `updated:` in the frontmatter..." from step 4 — MEMORY-FORMAT.md already instructs updating `agent:` on every invocation, and `updated:` is a template placeholder filled by definition whenever the template is used
- 2026-07-05 [Claude Code]: Merged steps 3 ("Update current state") and 4 ("Append snapshot to history") into a single "Write the snapshot" step, folding vault health triage (old 3b) in as a sub-bullet — `.cortex/MEMORY.md` was being written in three separate touches (3, 3b, 4) with the PRAXIS gate sandwiched in between; now PRAXIS (session-judgment, different file) runs once as step 3, then the whole MEMORY.md snapshot (both zones) is written in one pass as step 4; fixed step 2's "overwrite the frontmatter in step 3" to "step 4" accordingly
- 2026-07-05 [Claude Code]: Removed the entire ## Rules section from SKILL.md — "Language: use the locale resolved in step 1" duplicated LOCALE-RESOLUTION.md's own instruction, ".cortex/ must be in .gitignore" duplicated step 2's own action; moved the one rule with no duplicate elsewhere ("Don't duplicate content already in ADRs, PRDs, issues, or commits") to MEMORY-FORMAT.md, next to the other content-authoring rule (empty sections); fixed a stale "see SKILL.md step 5c" reference in MEMORY-FORMAT.md to "step 2" (5c no longer exists after the step-collapse)
- 2026-07-05 [Claude Code]: Step 3b: dropped the restated "5-item limit" number (already in MEMORY-FORMAT.md) while keeping the unique priority rule (vault-health evicts the least-recent item when Pending is full); dropped "(Output format, point 1)" — a Pending item is already reported under Output format's own point 1, the cross-reference added nothing
- 2026-07-05 [Claude Code]: Trimmed the step-1/step-3/step-4 no-ops surfaced during review — "(argument → CWD → default; prompt to run `/cortex-forge-setup` if missing)" and "(same criteria `cortex-forge-setup` uses to validate a vault)" and "(snapshot the project repo and update the linked vault page)" each restated content already fully covered by the reference being invoked in the same breath, or by ## Cross-vault update / step 5 further down; "(format, hard limits, zone semantics)" in step 3 likewise just named what's inside MEMORY-FORMAT.md; "(co-located with this skill)" in step 4 was the only one of 5 references/ mentions in the file carrying that qualifier — inconsistent, dropped
- 2026-07-05 [Claude Code]: Trimmed a third no-op from step 1 — "(snapshot the project repo and update the linked vault page)" repeated what step 5 and the full ## Cross-vault update section already say about cross-vault mode
- 2026-07-05 [Claude Code]: Trimmed two no-ops from step 1 surfaced during review — "(argument → CWD → default; prompt to run `/cortex-forge-setup` if missing)" restated VAULT-RESOLUTION.md's own fallback chain verbatim in the same breath as invoking it, and "(same criteria `cortex-forge-setup` uses to validate a vault)" was attribution, not an instruction — neither changed what the agent does
- 2026-07-05 [Claude Code]: Collapsed steps 1/1a/2/3 (repo, agent, vault, locale, mode — all detection, no side effects) into a single "Resolve context" step, and 4/5/5b/5c (`.cortex/` setup, read MEMORY.md, prune PRAXIS, rotate History) into a single "Prepare state" step — flowing prose instead of rigid lettered sub-steps, no instruction dropped; renumbered 6→3/3a/3b, 7→4, 8→5, fixed the stale "step 2b" locale reference (no such step existed) to "step 1", and "step 5" in Output format to "step 2"
- 2026-07-05 [Claude Code]: Moved step 6a's PRAXIS zone-classification gate (Confirmed/Provisional/Genuinely ambiguous) to references/PRAXIS-FORMAT.md's "When to write" section and compressed it there; step 6a in SKILL.md now points to the gate instead of reproducing it — cut from ~300 to ~68 words, the single largest word-count reduction in the file, with zero loss of decision logic (investigated and rejected moving 5b/5c/6b to a separate maintenance skill, per a Gemini-drafted proposal: those steps are cheap and mechanical, not what made the skill feel heavy — 6a alone was 37% of ## Steps by word count and the only step requiring full-session review; splitting out 5c would also have orphaned the auto-rotation this file's own references promise)
- 2026-07-05 [Claude Code]: Removed the ## Zone 1/## Zone 2 sections — fully duplicated by references/MEMORY-FORMAT.md (limits, agent: semantics, Current-state-vs-History rule) and, for the Imprint candidate paragraph, by AGENTS.md's session-start read logic; steps 6 and 7 now point directly at MEMORY-FORMAT.md for format/limits/zone semantics; also dropped 3 now-redundant Rules lines (empty-sections, pending-lives-in-current-state ×2) and moved the surviving "when in doubt" judgment line and the CONSOLIDATED.md read-note into MEMORY-FORMAT.md so nothing was lost
- 2026-07-05 [Claude Code]: Removed the ## Modes section — it duplicated step 3 (mode detection) and steps 4–8/Cross-vault update (mode behavior) with no new information; compressed step 1a's agent-detection instruction, dropping rationale clauses that didn't change the detection order
- 2026-07-05 [Claude Code]: Locale fallback (references/LOCALE-RESOLUTION.md) no longer checks the AGENTS.md Vault identity section — section eliminated from the suite
- 2026-07-05 [Claude Code]: Compressed the flavor-line instruction to a single sentence — removed the duplicated position instruction and the "literally" vs "translated" contradiction
- 2026-07-04 [Claude Code]: Added process-tree walk (`$PPID` upward via `ps -o comm=`) as a fallback method for detecting the invoking agent in step 1a when self-knowledge and env vars aren't enough — rescued from a finding in `wiki/meta/agent-diagnostics.md` (2026-06-11) about CommandCode, which exposes no self-identifying environment variable
- 2026-07-04 [Claude Code]: Added step 6b (vault health triage) that propagates findings from `vault-report.json` to `### Pending` on every crystallize, closing a gap where nobody recorded whether the vault health triage (AGENTS.md step 3) was actually addressed
- 2026-07-04 [Claude Code]: Centralized vault structure validation (`wiki/`+`AGENTS.md`) in `references/VAULT-RESOLUTION.md`; the standard-vs-cross-vault mode detection in step 3 is unrelated routing logic and was left untouched
- 2026-07-04 [Claude Code]: Reworded "Resolve vault" step intro to distinguish VAULT-RESOLUTION.md (decision flow) from LOCALE-RESOLUTION.md (fallback chain), removing the repeated closing phrase
- 2026-07-04 [Claude Code]: Added an explicit "safe to end" verdict to the output format, inspired by a comparative analysis with the `stow` skill (from another repo), so malformed frontmatter or unresolved context doesn't stay buried only in History
- 2026-07-04 [Claude Code]: Extracted "Resolve vault" logic to shared `references/VAULT-RESOLUTION.md`, co-located across 5 skills (was duplicated inline with real drift between copies)
- 2026-07-04 [Claude Code]: Added a third "genuinely ambiguous" case to the step 6a PRAXIS.md gate — ask the user once instead of guessing, inspired by a comparative analysis with the `stow` skill; also removed a parenthetical historical-context phrase ("confidence, not category, decides") from the same gate
